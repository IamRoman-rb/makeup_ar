/// Plantilla para tus license keys de DeepAR.
///
/// Copiá este archivo como `deepar_keys.dart` (mismo directorio) y pegá ahí
/// tus keys reales. `deepar_keys.dart` está en `.gitignore` — nunca se sube
/// al repo, así que cada máquina/CI necesita su propia copia local.
class DeepArKeys {
  const DeepArKeys._();

  static const String android = 'TU_LICENSE_KEY_DE_ANDROID';
  static const String ios = 'TU_LICENSE_KEY_DE_IOS';
}
